#!/usr/bin/env bash

INFO=""

command -v $1 2>&1 > /dev/null
if [[ $? -eq 0 ]]; then
    INFO="VPN: $(mullvad status) ---"
fi

INFO+=" $(upower -i /org/freedesktop/UPower/devices/battery_BAT0 | grep 'percentage' | xargs | cut -d ' ' -f 2)"
INFO+=" $(date +'%d-%m-%Y %H:%M:%S')"

echo "$INFO" 
